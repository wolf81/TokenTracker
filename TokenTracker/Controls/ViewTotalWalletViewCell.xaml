<?xml version="1.0" encoding="UTF-8"?>
<ViewCell
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="TokenTracker.Controls.ViewTotalWalletViewCell">
    <ViewCell.View>
        <Grid
            HeightRequest="50"
            Margin="20,0,0,0"
            ColumnSpacing="10"
            RowSpacing="0">
            <Grid.RowDefinitions>
                <RowDefinition
                    Height="1" />
                <RowDefinition
                    Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition
                    Width="*" />
                <ColumnDefinition
                    Width="*" />
            </Grid.ColumnDefinitions>
            <ContentView
                BackgroundColor="DarkGray"
                VerticalOptions="FillAndExpand"
                Grid.Row="0"
                Grid.ColumnSpan="2" />
            <Label
                Grid.Row="1"
                Grid.Column="0"
                Text="Total"
                FontSize="Medium"
                HorizontalOptions="FillAndExpand"
                VerticalOptions="FillAndExpand"
                VerticalTextAlignment="Center"
                HorizontalTextAlignment="Start" />
            <Label
                Grid.Row="1"
                Grid.Column="1"
                Text="{Binding Price, StringFormat='{0:0.00}'}"
                FontSize="Medium"
                Margin="0,0,20,0"
                HorizontalOptions="FillAndExpand"
                VerticalOptions="FillAndExpand"
                VerticalTextAlignment="Center"
                HorizontalTextAlignment="End" />
        </Grid>
    </ViewCell.View>
</ViewCell>
